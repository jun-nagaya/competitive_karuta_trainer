"""
共通定数
- コメントは現状の目的・契約・使い方のみを記載する。
"""

# ミュート時の文字ストリーミング表示の1文字あたり遅延秒
STREAMING_CHAR_DELAY: float = 0.1

# データセットに期待するファイルの候補
# ファイル名による固定は行わず、ローダ側で内容検査により判定するため、
# ここでは空の定義のみを残す（互換目的）。
FILE_ALIASES: dict[str, list[str]] = {}
